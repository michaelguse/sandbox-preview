<!DOCTYPE html>
<html lang="en">
<% include ../partials/head.ejs %>

  <body>
    <div class="wrapper">
      <div class="body-wrapper">
        <% include ../partials/nav.ejs %>
          <% include ../partials/header.ejs %>
          <% results.forEach(function(r) { %>
            <div class="content-wrapper">
              <div class="instance-display">
                <div class="instance-block">
                  <!-- Instance block -->
                  <p class="instance-header">Instance</p>
                  <p class="instance-text">
                    All
                  </p>
                </div>
                <div class="instance-type-block">
                  <!-- Instance type block-->
                  <p class="instance-header">Instance Type</p>
                  <p class="rel-type-text">
                    All
                  </p>
                </div>
                <div class="release-name-block">
                  <!-- Release name block -->
                  <p class="instance-header">Release</p>
                  <p class="rel-type-text">
                    <%= r.external_rel_name  %>
                  </p>
                </div>
                <div class="release-logo-block">
                    <!-- Release logo block -->
                    <% if (r.internal_rel_name == 218) {  %>
                    <img src="/images/sf_rel_logo_spring19.png" height="50" width="50" alt="Winter '19 Logo" />
                    <% } %>
                    <% if (r.internal_rel_name == 216) { %>
                    <img src="/images/sf_rel_logo_winter19.png" height="50" width="50" alt="Winter '19 Logo" />
                    <% } %>
                </div>
              </div>
              <% }); %>
              <div class="component">
                <h3>What is your Sandbox instance?</h3>
                <form action="/upgrade" type="GET" class="form-primary">
                  <% if (locals.errors) { %>
                    <input class="form-error-msg" name="org_id" id="org_id" placeholder="" type="text" autocomplete="off" autofocus required />
                    <input type="submit" class="button" value="submit" />
                    <% errors.forEach(function(r) { %>
                      <br/><label for="org_id" style="color: red"><%= r %></label>
                  <% }); %>
                  <% } else { %>
                    <input name="org_id" placeholder="Instance name, eg CS87" type="text" autocomplete="off" autofocus required />
                    <input type="submit" class="button" value="submit" />
                  <% }; %>
                </form>
                <div class="general-content">
                  <h4>Unsure of your Sandbox Instance?</h4>
                  <p>To determine which instance your Sandbox is on, log into your production org and navigate to your Sandbox
                    List page (Search "Sandbox" in the Quick Find bar on the Setup Home Page).</p>
                  <img src="/images/listpage.png" alt="list page" />
                  <p>Your sandbox instance is listed in the
                    <strong>Location</strong> column above.</p>
                </div>
                <div class="divider"></div>
                <% include ../partials/helpful-links.ejs %>
              </div>
            </div>
      </div>
      <% include ../partials/footer.ejs %>
        <% include ../partials/script_section.ejs %>
    </div>
  </body>

</html>